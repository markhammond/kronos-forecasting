bin/
obj/
*.user
*.suo
.vs/
.idea/
artifacts/

# Checkpoints are fetched, never committed: the Small pair alone is ~110 MB and git
# history is append-only. scripts/fetch-checkpoints.sh retrieves the pinned revisions.
checkpoints/
*.safetensors

# macOS Finder metadata
.DS_Store

# Fetched, not vendored — Google's Apache-2.0 sources and the tensors they generate.
reference/timesfm3/
reference/parity.safetensors
